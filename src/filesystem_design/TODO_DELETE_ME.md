# TODO

Rewrite all references about the caller needing to set the root directory on the root disk. This is now the responsibility the process that creates the initial pool, as that directory is now on the second disk.

Remove all un-needed data from disk headers.